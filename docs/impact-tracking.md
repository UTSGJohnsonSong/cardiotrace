# 影响追踪 —— 合并门禁

> **这份文件回答一个问题：每个被修掉的 bug，有没有污染过已经发布出去的数字。**
>
> 「修好了」和「从来没错过」是两件事。修复能保证以后对，不能回答**过去发布的东西现在还算不算数**——
> 一个只列出修了什么的清单，读起来像后者，实际只说了前者。
>
> 因此每条都必须落在三个结论之一：
>
> | 判定 | 含义 | 后续动作 |
> |---|---|---|
> | **已污染** | 对外发布过错的数或错的结论 | 必须重算并核对旧值→新值，本表记录两者 |
> | **verified no-impact** | 代码确实危险，但**有证据表明**它从未在已发布路径上触发 | 记录证据，不重算 |
> | **已证伪** | 报告的缺陷本身不成立 | 记录反证，不改代码 |
>
> **「看起来没影响」不是一个判定。** 没有证据的一条，判定栏留空，不得合并。
>
> 审查基线：`analysis-refresh` 分支，2026-08-24。
> 数字出处：`reports/descriptive_results.json` · `reports/model_results.json` ·
> `reports/tables/`。复现见每条的「证据」列。

---

## A. 已污染 —— 发布过的数字是错的

这七条改变了对外可见的内容。旧值一并留着，因为「这个数变过」本身是读者需要知道的。

| # | 缺陷 | 旧（已发布） | 新（当前） | 证据 |
|---|---|---|---|---|
| A1 | `concordance()` 接受 `weights=` 但函数体从不读它，调用方也没传 | 10 年 Harrell C = **0.804** | **0.838** 加权，0.804 作未加权对照保留 | `reports/model_results.json` → `prediction.*.harrell_c` / `harrell_c_unweighted`。同时 10 年平均预测风险 2.82% → **1.96%** |
| A2 | Part 1 用体检权重（`WTMEC2YR`）估计只来自访谈的结局 | 疫情后周期「损失 22.3% 样本」被当作趋势解释的一部分 | 换 `WTINT2YR` 后该损失 **≤0.05%**，这条竞争性解释消失 | `src/descriptive.py:WEIGHT_INTERVIEW` · `reports/tables/part1_flow.csv` 与 `part1_flow_examweight.csv` 并列保留两条阶梯 |
| A3 | 趋势区间用正态分位 1.96，而设计自由度只有 8 | 95% CI **−1.16 到 −0.06 pp**，报告写「区间不含零，所以下降不是噪声」 | t(8)=2.306 → **−1.218 到 +0.035 pp**，`std_slope_excludes_zero: false` | `reports/descriptive_results.json` → `part1.std_slope_ci` / `slope_dof` / `slope_t_crit`。**这条改的不是数字，是结论**——原句现由该布尔字段驱动 |
| A4 | 血压 HR 以 `hr ** 10` 缩放，而 `hr` 已被四舍五入到 4 位 | **1.121** | **1.122**（`exp(coef×10)` = 1.1216） | `1.0115 ** 10 = 1.121137` vs `exp(coef*10) = 1.121588`。四舍五入被复利十次，进到报告打印的第三位。测试：`tests/test_estimator_contracts.py::test_a_scaled_hazard_ratio_starts_from_the_log_scale` |
| A5 | `scripts/pce_variable_cascade.py` 自建排除阶梯，比 STROBE 少三级 | 完整病例子样本 **17,464 人 / 756 事件**，写进四处文档并进了一条已锁定的协议决定 | **18,744 人 / 824 事件** | 它从 20,771 起步（漏了 linkage 资格、MEC 完成、死因编码），分析样本是 20,736。现改为直接读 `strobe_part3.csv` 与 `cohort_part3.csv.gz`，测试 `test_the_pce_cascade_starts_where_the_strobe_ladder_ends` 钉住接缝 |
| A6 | 两处 `decision()` 调用漏了 `f` 前缀 | 线上页面直接印出 `{...}` 模板变量 | 渲染为 0.85 / 5.6 | 目视 + `tests/test_report.py` 断言无 `{…}` 残留 |
| A7 | 候选表空字符串 → CSV → 读回为 `NaN`，`bool(nan)` 为真 | 页面上 **9 个 `nan`** | 两端都修 + 守卫测试 | `tests/test_report.py` 的 nan 守卫；构建后扫描 `docs/*.html` 现为 0 |

**共同点值得记下**：A1、A3、A4 都不是「算错了」，是**接口默默接受了一个它不使用的参数、或在错误的尺度上做了一次运算**。
三条都不会报错，都返回合理数值，都需要一个独立参照才能发现。

---

## B. verified no-impact —— 代码危险，但有证据表明从未触发

这四条是你上一轮确认可以记为 verified no-impact 的。**证据在第三列，不是「看起来没事」。**

| # | 缺陷 | 为什么危险 | 证据 |
|---|---|---|---|
| B1 | `prepare()` 就地加 Tobin 常量，重复调用会累加（140 → 150 → 160） | 二次调用会把服药者血压推高一倍校正量，且不报错 | 逐个检查 **5 处调用点，无一嵌套**。现加守卫：帧里已有 `design_cluster` 即抛错，重复调用不再可能。守卫 + 全绿测试构成证明，不是缺席证明 |
| B2 | `HorizonClassifier.predict_cif` 忽略传入的 horizon | 用 5 年模型报 10 年风险，数值合理、无报错 | 仅 `HorizonClassifier` 受影响；`CauseSpecificRisk` 路径本来就正确。2026-08-24 在全部非训练周期（n = 13,964，其中 1,394 人因完整病例缺失为 `NaN`）上实测 CIF：1/5/10/15 年 **0.174 / 0.928 / 2.785 / 5.479%**（未加权），**13,964 人中 0 人出现随时间下降**。现 horizon 不匹配即抛错 |
| B3 | 分层 cluster bootstrap 丢弃事件不足的重抽样，但不报告丢了多少 | 若大量丢弃，报出的 `n_boot` 就是假的 | 四条 arm 全部 **400/400，零丢弃**。现返回 `n_boot` / `n_boot_requested` / `n_boot_dropped` 三个字段，丢弃比例超过一半即抛错 |
| B4 | `make all` 曾让 `descriptive` 先于 `learning`，报告读到上一轮的 Part 4 产物 | 一轮陈旧、无报错、无信号 | `cohort` 独立成 target，`all` 明确排序。**并由 `make verify` 机械保证**：干净重建后 `git status` 必须为空，否则失败。这条从「已核对」升级为「有机制挡住」 |

---

## C. 已证伪 —— 报告的缺陷不成立

留在这里是因为「查过了、不成立」和「没查」对读者是两回事。

| # | 报告的缺陷 | 反证 |
|---|---|---|
| C1 | `design_cluster` 的 strata×psu 编码会碰撞 | 实测 **241 个编码，0 个跨层共享**。编码方式本身不产生碰撞 |
| C2 | 校准十分位是未加权的 | `calibration_table` 本来就用加权分位切点。**但**切点此前由 `pd.qcut` 给出（按人数等分），已改为按累积权重等分——这是另一个问题，不是报告的那个 |
| C3 | `predict_cif` 用 S(u) 而非 S(u−)，高估绝对风险 | 方向对，**量级可忽略**：10 年平均预测风险 1.96% 上约 **+0.0040 个百分点**。仍然改了代码，理由是公式与实现必须一致，不是因为数字错 |

---

## D. 本轮新发现，未对外发布过

修的时候撞见的，从没进过已发布产物，因此不占前三节。

| # | 缺陷 | 后果（若未发现） |
|---|---|---|
| D1 | `render_readme.py` 的正文测试数同步正则里，`\b` 被二次转义成真正的退格符（`\x08`） | 替换永远匹配不上，却报告「同步了 0 处」——看起来和「本来就没有可同步的」完全一样。徽章写 164，正文三处写 85，两边都「通过」。现加断言：匹配到 0 处即失败 |
| D2 | `verify_clean_rebuild.py` 用系统代码页（此机为 GBK）解码子进程输出 | 任何一个 en dash 就让读取线程抛 `UnicodeDecodeError`，捕获到的输出为空。**真失败会被报成一行没有下文的错误** |
| D3 | 三个 sub-heading 之外，`research-design.md` 还有 **6 个** 带锁标记的小节标题 | 审计只找出两套状态系统，实际是三套。由 `test_no_section_heading_carries_its_own_status` 当场抓出 |

---

## E. 合并前仍需人工确认

1. **A5 改了一条已锁定协议里的数**（`research-design.md` 决策记录 2026-08-19 第②条）。
   按层级，决策记录是历史、不重写，所以那一行加了「记录更正」而非改动原文。
   **约束本身没变**（「必须在同一子样本上重评」），变的是 n。这个处理方式需要你确认。
2. **A2 使 Part 1 与 Part 3 使用不同权重**。规则是「取最严格组件」，已写进 README 与 briefing。
   若你认为应统一到体检权重，Part 1 的全部数字要重算。
3. **A3 把一条结论从「有」改成「无」**。报告现在说趋势区间含零。
   这是本轮唯一一处**结论方向**改变，值得在合并说明里单独点名。
4. **PCE 头对头仍未实现**（节点 15 = 🔄）。协议已锁定，A5 修正了它引用的 n，但对比本身没做。
   若合并说明里出现「PCE 基准」字样，需要说清是协议已定、实现未做。
5. **重建验证的覆盖范围与时点，逐条写清**——上一版这里写「见合并当日的运行记录」，
   而那份记录并不存在。一条指向不存在证据的断言，和没有断言不是一回事，是更糟。

   | 范围 | 跑过没有 | 在哪个 commit | 谁保证 |
   |---|---|---|---|
   | `--render`（报告 / 站点 / README） | ✅ | `691ebfd`（当前 HEAD） | **CI 每次 push**（`.github/workflows/ci.yml`） |
   | 默认范围（+ 表、图、生存模型） | ✅ | `691ebfd` | 仅本地 |
   | `--full`（+ Part 4） | ✅ | **`1d30cb4`，不是当前 HEAD** | 仅本地 |

   `--full` 之后又落了三个 commit（`b73d08f` `21b48e0` `ed50ebb` `691ebfd`），
   其中 `691ebfd` 改了 `render_report.py`。**合并前必须在最终 commit 上重跑一次 `--full`**，
   否则 Part 4 产物与代码是否一致，这轮没有任何证据。

   `--full` 进不了 CI 的原因是它需要 `data/processed/`，而该目录被 gitignore。
   **Part 4 产物的重建由本地负责，不由 CI 保证**——这是一条结构性缺口，不是疏忽。
6. `verify_clean_rebuild.py` 目前不留运行凭据。第 5 条那张表是我手填的，
   下次仍会过期。**待办**：让它把「时间 · commit · 范围 · 结果」写进
   `reports/verify_receipt.json`（比照 `test_summary.json`，并加入 `IGNORE_PATHS`），
   由文档引用该文件而不是引用一段记忆。

---

_本文件是合并门禁，不是变更日志。变更日志在 git 历史里；
决策在 [`research-design.md`](research-design.md)；数字在 `reports/`。_
